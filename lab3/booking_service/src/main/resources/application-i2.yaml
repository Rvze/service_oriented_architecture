server:
  port: 8084
spring:
  application:
    name: booking-service
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8661/eureka
  instance:
    hostname: localhost
    instance-id: ${spring.application.name}_i2